name: Build KuroNeko Extreme Kernel (Arch)

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    container:
      image: archlinux:latest

    steps:
      - name: Checkout Source Code
        uses: actions/checkout@v4

      - name: Install Dependencies
        run: |
          # Обновляем базы данных
          pacman -Syu --noconfirm
          # Ставим все необходимое, включая компиляторы
          # arm-none-eabi-gcc используется для 32-битной части ядра (vDSO)
          pacman -S --noconfirm base-devel aarch64-linux-gnu-gcc arm-none-eabi-gcc bc bison flex cpio python openssl wget

      - name: Build Kernel
        run: |
          # Патчим percpu.h, чтобы убрать ошибку сборки
          sed -i 's/BUILD_BUG();/\/* BUILD_BUG(); *\//g' arch/arm64/include/asm/percpu.h

          # Настройка окружения
          export ARCH=arm64
          export CROSS_COMPILE=aarch64-linux-gnu-
          # Указываем путь к 32-битному компилятору
          export CROSS_COMPILE_ARM32=arm-none-eabi-
          
          # Очистка и конфигурация
          make clean && make mrproper
          make akx_extreme_defconfig
          make olddefconfig
          
          # Сборка с флагами подавления ошибок
          make -j$(nproc) KBUILD_CFLAGS='-w -Wno-error' HOSTCFLAGS='-w -Wno-error'

      - name: Upload Artifact
        uses: actions/upload-artifact@v4
        with:
          name: KuroNeko-Extreme-Artifact
          path: arch/arm64/boot/Image.gz-dtb

      - name: Create Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: "KuroNeko-v${{ github.run_id }}"
          name: "KuroNeko Extreme Kernel Build"
          body: |
            ### 🥟 Автоматическая сборка ядра KuroNeko Extreme
            * **Особенности:** Вшит KernelSU-Next (Legacy) без gki.
            * **Окружение:** Arch Linux Container.
          files: arch/arm64/boot/Image.gz-dtb
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
